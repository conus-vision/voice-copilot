[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[project]
name = "voice-copilot"
version = "0.1.0"
description = "Voice-first narrator and control layer for LLM coding CLIs"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Volodymyr Moskvin", email = "info@conus.vision" }]
maintainers = [{ name = "Conus Vision", email = "info@conus.vision" }]
keywords = ["llm", "cli", "voice", "tts", "stt", "claude", "codex", "pair-programming"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Environment :: Web Environment",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
    "Topic :: Software Development",
]
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",
    "pyyaml>=6.0",
    "typer>=0.12",
    "rich>=13.7",
    "anthropic>=0.40",
    "openai>=1.50",
    "httpx>=0.27",
    # Codex compresses its Responses request bodies with zstd; the proxy has to
    # decompress them to read the user's question out of the turn.
    "zstandard>=0.22",
    "edge-tts>=6.1",
    "pynput>=1.7",
    "pystray>=0.19",
    "pillow>=10.3",
    "platformdirs>=4.2",
    "keyring>=25.0",
    "python-dotenv>=1.0",
    "psutil>=5.9",
    "ptyprocess>=0.7 ; sys_platform != 'win32'",
    "pywinpty>=2.0 ; sys_platform == 'win32'",
]

[project.optional-dependencies]
local-tts = [
    "torch>=2.2",
    "torchaudio>=2.2",
    "numpy>=1.26",
    "soundfile>=0.12",
]
local-stt = [
    "faster-whisper>=1.0",
    "numpy>=1.26",
    "soundfile>=0.12",
]
deepgram = ["deepgram-sdk>=3.7"]
elevenlabs = ["elevenlabs>=1.5"]
all = [
    "voice-copilot[local-tts,local-stt,deepgram,elevenlabs]",
]
dev = [
    "ruff>=0.6",
    "mypy>=1.11",
    "pytest>=8.3",
    "pytest-asyncio>=0.23",
    "types-pyyaml",
]

[project.scripts]
voice-copilot = "voice_copilot.cli:main"

[project.urls]
Homepage = "https://conus.vision"
Repository = "https://github.com/conus-vision/voice-copilot"
Issues = "https://github.com/conus-vision/voice-copilot/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/voice_copilot"]

[tool.hatch.build.targets.sdist]
# Ship only what is needed to build/install; keep docs, tests and media
# out of the published source distribution.
include = [
    "/src/voice_copilot",
    "/README.md",
    "/LICENSE",
    "/pyproject.toml",
]

[tool.ruff]
line-length = 100
target-version = "py311"
# Verbatim excerpts of upstream headroom code, kept only as a drift baseline for
# scripts/check_headroom_updates.py — not our code, don't lint/format it.
extend-exclude = ["docs/headroom-porting/snapshots"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_ignores = true
ignore_missing_imports = true
mypy_path = "src"
exclude = ["docs/headroom-porting/snapshots/"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
